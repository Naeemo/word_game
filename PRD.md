# 小小键盘单词机（Word Keys） - 产品需求文档（PRD）

> 版本：v1.0 | 作者：Kimi | 日期：2026-07-18
> 状态：草稿（待家长确认）

## 1. 概述

### 1.1 背景与动机

为一名 4 岁+ 儿童做一个本地单词游戏：孩子敲键盘上的字母键，页面出现以该字母开头的单词、图片，并依次朗读单词、中文释义、例句、例句中文，同时积分 +1。每个字母对应的单词不固定，从家长提供的带优先级单词清单中，按"出现次数 + 记忆曲线"逻辑调度出现频率。仅在本地 Mac 上使用，不上线、不分发。

孩子基线：已认识 26 个字母，能拼读一些单词，幼儿园有全职外教，日常英语暴露充分。

教育定位：**不追求明确可测的学习目标**。核心诉求是孩子喜欢玩、对单词越来越熟悉即可。因此设计上明确排除测验、对错、掌握度判定等机制。

### 1.2 目标

- 用户目标（孩子）：敲键盘立刻有有趣的反馈（图 + 声音 + 加分），愿意反复玩。
- 用户目标（家长）：词表、例句、中文释义、每轮词数可控；单词出现频率符合"多次曝光、间隔拉开"的记忆规律，而非纯随机。
- 成功指标：孩子主动要求再玩；单词按调度逻辑重复出现（可后台验证曝光计数）。

### 1.3 范围

- 本期包含：按键出词、单词卡（图+文）、四段式朗读、防连击、积分、曝光调度算法、配置页、结算页、基础动画、本地图片与词表。
- 本期不包含：测验/对错机制、多孩子档案、云端同步、Electron 桌面打包、在线图片搜索。

## 2. 用户画像

**角色：小玩家（孩子，4 岁+）**
- 身份：游戏唯一玩家，认识字母、有初步拼读能力
- 核心目标：敲键盘、看到好玩的图、听到声音、攒积分
- 主要痛点：注意力短，乱敲键盘是本能；看不懂复杂 UI；重复看到一模一样的内容比内容变化更安心（家长确认：同一词换图会让孩子困惑，故一词一图固定）

**角色：家长（内容策展人 + 游戏管理员）**
- 身份：孩子的父亲，非专业教育者/开发者
- 核心目标：提供带优先级的单词清单和例句；控制每轮游戏时长；孩子玩的时候不用盯着
- 主要痛点：没时间做内容工程（图片、翻译等希望尽量自动化）；不希望游戏有"玩不完"的成瘾设计

## 3. 用户故事

**小玩家：**
- US-K1：作为孩子，我想要按下任意字母键立刻看到图片和单词，以便觉得"我按键，屏幕就听我的"。
- US-K2：作为孩子，我想要听到单词和句子的朗读（英文+中文），以便把声音和画面联系起来。
- US-K3：作为孩子，我想要每按一次就加分、看到积分变多，以便获得即时成就感。
- US-K4：作为孩子，我想要玩完一轮后看到庆祝画面，以便有"我完成了"的满足感。

**家长：**
- US-P1：作为家长，我想要用一份 JSON 清单管理单词、优先级、例句和中文，以便完全控制孩子接触的内容。
- US-P2：作为家长，我想要配置"一轮玩多少词"，到量后自动进入"今日已结束"结算页，以便控制屏幕时间。
- US-P3：作为家长，我想要单词按"出现次数 + 递增间隔"重复出现，而不是纯随机，以便孩子对新词有多次、间隔拉开的曝光。
- US-P4：作为家长，我想要整个游戏离线运行、双击即开，以便不依赖网络、不产生任何外部数据。

## 4. 功能需求

### 4.1 功能清单

| 编号 | 功能名称 | 来源故事 | 描述 |
|------|----------|----------|------|
| F-001 | 按键出词 | US-K1 | 监听 a-z 键，从对应字母词池按调度算法选词展示；非字母键无响应 |
| F-002 | 单词卡展示 | US-K1 | 图片 + 大写单词 + 首字母高亮（与键盘大写印刷一致） |
| F-003 | 四段式朗读 | US-K2 | Web Speech API 依次朗读：单词(EN) → 单词中文 → 例句(EN) → 例句中文 |
| F-004 | 防连击 debounce | US-K1 | 当前词序列（展示+朗读）未结束时，一切按键忽略；长按不累积 |
| F-005 | 积分与持久化 | US-K3 | 每成功出词 +1，实时写入 localStorage，刷新不丢 |
| F-006 | 曝光调度算法 | US-P3 | 每词记录曝光次数，按递增间隔冷却；优先级加权随机；详见 §4.4 |
| F-007 | 词表加载 | US-P1 | 启动时加载本地 JSON 词表（字段见 §4.5），格式错误时明确报错 |
| F-008 | 一词一图 | US-K1 | 每词固定一张本地图片，离线可显示，重复出现不换图；真实图片未就绪前用 emoji 占位图 |
| F-009 | 家长配置页 | US-P2 | 配置每轮词数（默认 20）、中文朗读开关（默认开）、语速（慢/正常） |
| F-010 | 结算页 | US-K4, US-P2 | 达到本轮词数后自动进入"今日已结束"页：庆祝动画 + 本轮回顾 + 总积分；页面无任何可见继续入口，家长经隐藏手势（长按左上角 3 秒）回配置页 |
| F-011 | 适度动画 | US-K1, US-K3 | 单词卡 spring 弹入、首字母高亮、+1 飘分；单次 ≤1s，克制不花哨 |
| F-012 | 稀有字母兜底 | US-K1 | X/Q/Z 等允许"词中含此字母"（fox 含 x），选词时 initial 优先于 contains |
| F-013 | 全屏防误触 | US-P4 | 游戏主界面全屏运行，尽量减少浏览器 UI 与系统快捷键干扰 |
| F-014 | 点击重播 | US-K2 | 序列播放完后，点击图片/喇叭按钮可重读完整序列，重播不叠音 |
| F-015 | 本轮进度条 | US-K4 | 显示 n/N 进度，满格时播放满格动画再进结算页 |
| F-016 | 曝光统计页 | US-P3 | 家长可查看每词曝光次数，验证调度效果 |
| F-017 | 音效 | US-K3 | 按键音、加分音（独立音量开关） |
| F-018 | 多孩子档案 | — | 明确不做（只有一个孩子） |
| F-019 | 桌面打包 | — | 明确不做（本地 Web 应用够用） |
| F-020 | 测验/对错机制 | — | 明确不做（家长已定：敲就有响应，无对错） |
| F-021 | 在线账号/云同步 | — | 明确不做（纯本地） |

### 4.2 非功能需求

| 维度 | 要求 |
|------|------|
| 性能 | 按键到卡片出现 ≤100ms；图片本地加载 |
| 可用性 | 完全离线可用（首次构建后无任何网络依赖） |
| 兼容性 | macOS 最新版 Chrome/Safari；TTS 语音构建期在目标机器实测选定 |
| 易用性 | 孩子侧零文字依赖、零学习成本；所有配置收敛到一个家长页面 |
| 内容安全 | 无外部链接、无广告、无第三方追踪；图片逐张人工审查 |
| 可维护性 | 词表、图片均为数据文件，增删词不改代码 |
| 数据 | localStorage 持久化积分、曝光计数、配置；接受被清理即重置的低风险 |

### 4.3 功能依赖关系

- F-007（词表）+ F-008（图片）是 F-001/F-002/F-003 的前置
- F-006（调度）依赖 F-007；F-001 依赖 F-006
- F-004、F-005、F-011 依附于 F-001 主流程
- F-010 依赖 F-005、F-009
- F-012 是 F-001 的选词扩展，可后置

### 4.4 曝光调度算法设计（F-006）

约束背景：游戏无对错信号，无法用经典 SRS（Anki/SM-2）。采用**曝光计数 + 递增间隔冷却**的简化模型：

- 每个词维护两个状态：`exposureCount`（已出现次数）、`nextEligibleAt`（第几次全局出词后可再次出现）
- 间隔序列（按全局出词数计，可在词表 JSON 顶部配置）：`[3, 6, 12, 24, 48]`
  - 第 1 次曝光后，间隔 3 次出词才可再出现；第 2 次后间隔 6；依此类推，封顶 48
- 选词流程（孩子按下字母 L）：
  1. 候选 = L 池中所有 `nextEligibleAt ≤ 当前全局出词计数` 的词（未曝光词恒为 eligible）
  2. 在候选中按**优先级加权随机**：权重 = 词表中的 priority 值
  3. 若候选为空（全部冷却中），取"距离解冻最近"的词直接解冻——保证**敲键必有响应**（家长的硬约束优先于调度纯洁性）
- 新词引入：未曝光词天然 eligible，优先级高者统计上更早被抽中
- 已知局限：孩子选字母是随机的，冷却按全局计数而非每字母计数，冷门字母的词实际间隔会更长。可接受（符合"熟悉即可"的目标）。

### 4.5 词表数据格式（F-007）

```jsonc
{
  "settings": { "intervals": [3, 6, 12, 24, 48] },
  "words": [
    {
      "word": "apple",
      "letter": "a",
      "matchType": "initial",      // initial=以此字母开头；contains=词中含此字母（fox→x）
      "priority": 100,             // 家长排的优先级，越大越优先
      "zh": "苹果",
      "sentenceEn": "I eat an apple.",
      "sentenceZh": "我吃苹果。",
      "image": "images/apple.png"
    }
  ]
}
```

### 4.6 技术方案

- 形态：本地 Web 应用（浏览器全屏使用）
- 技术栈：Vite + React + TypeScript + Tailwind CSS + framer-motion（动画）
- 朗读：浏览器内置 Web Speech API（`speechSynthesis`），en-US 读英文、zh-CN 读中文；语速默认 0.9
- 存储：localStorage（积分、每词曝光状态、家长配置）
- 素材：`words.json` + `images/` 本地目录
- 运行：`npm run dev` 本地启动；构建产物可用任意静态服务器打开

## 5. 优先级

### 5.1 MoSCoW 矩阵

| 编号 | 功能 | 优先级 | 理由 |
|------|------|--------|------|
| F-001 | 按键出词 | Must | 核心玩法 |
| F-002 | 单词卡展示 | Must | 核心反馈 |
| F-003 | 四段式朗读 | Must | 家长明确的完整链路 |
| F-004 | 防连击 | Must | 家长明确指定，否则积分机制失效 |
| F-005 | 积分持久化 | Must | 核心激励 |
| F-006 | 曝光调度算法 | Must | 区别于纯随机的核心诉求 |
| F-007 | 词表加载 | Must | 内容来源 |
| F-008 | 一词一图 | Must | 家长明确：换图会让孩子困惑 |
| F-009 | 家长配置页 | Must | 家长明确指定（每轮词数） |
| F-010 | 结算页 | Must | 家长明确指定（今日已结束） |
| F-011 | 适度动画 | Must | 家长明确："别太干巴" |
| F-012 | 稀有字母兜底 | Should | X/Q/Z 体验，可用薄词表临时替代 |
| F-013 | 全屏防误触 | Should | 体验优化，浏览器能力有限，尽力而为 |
| F-014 | 点击重播 | Should | 常见诉求，但 v1 可没有 |
| F-015 | 本轮进度条 | Should | 增强完成感，结算页已部分覆盖 |
| F-016 | 曝光统计页 | Could | 家长向验证工具 |
| F-017 | 音效 | Could | 锦上添花 |
| F-018 | 多孩子档案 | Won't | 只有一个孩子 |
| F-019 | 桌面打包 | Won't | 本地 Web 已满足 |
| F-020 | 测验/对错 | Won't | 与教育定位冲突 |
| F-021 | 云同步 | Won't | 纯本地 |

Must 占比 11/21 ≈ 52%，符合 ≤60% 约束。

### 5.2 版本规划建议

- MVP（v1.0）：全部 Must
- v1.1：F-012 ~ F-015
- v2.0：评估 F-016、F-017

## 6. 验收标准

### F-001 按键出词

- AC-F001-01：Given 游戏主界面空闲，When 按下字母键 "a"，Then 100ms 内从 A 池选出一个词并展示单词卡
- AC-F001-02：Given 主界面，When 按下非字母键（数字、方向键、功能键），Then 无响应且不加分
- AC-F001-03：Given 词表中某字母无词条，When 按下该字母，Then 显示友好提示（如"还没有这个词哦"），不崩溃、不加分

### F-002 单词卡展示

- AC-F002-01：Given 出词 apple，When 卡片展示，Then 显示图片 + 大写 "APPLE"，且首字母 A 高亮
- AC-F002-02：Given 某词图片文件缺失，When 出词，Then 显示占位图 + 单词文字，流程不中断
- AC-F002-03：Given 不同图片原始尺寸不一，When 展示，Then 统一缩放填充、不变形、不溢出卡片

### F-003 四段式朗读

- AC-F003-01：Given 出词 apple 且配置开启中文，When 卡片出现，Then 依次朗读 "apple" → "苹果" → 英文例句 → 例句中文，顺序不乱、不重叠
- AC-F003-02：Given 系统无可用 TTS 语音，When 出词，Then 卡片正常展示、积分正常累加，流程不被朗读阻塞
- AC-F003-03：Given 某词条缺例句中文，When 朗读，Then 跳过该段、其余段正常
- AC-F003-04：Given 配置关闭中文朗读，When 出词，Then 仅朗读单词与英文例句

### F-004 防连击

- AC-F004-01：Given 某词序列（展示+朗读）进行中，When 连续快按多个字母键，Then 全部忽略，画面与积分不变
- AC-F004-02：Given 当前词序列播放完毕，When 再按字母键，Then 正常出下一个词
- AC-F004-03：Given 长按某字母键（系统重复触发 keydown），When 序列进行中，Then 不累积任何触发

### F-005 积分与持久化

- AC-F005-01：Given 空闲状态，When 成功出一个词，Then 积分 +1 并立即写入 localStorage
- AC-F005-02：Given 已有积分 37，When 刷新页面或重启浏览器，Then 积分仍显示 37
- AC-F005-03：Given 同一词重复出现，When 出词，Then 同样 +1（无对错，重复曝光也计分）

### F-006 曝光调度算法

- AC-F006-01：Given 词 apple 刚出现且间隔未到，When 其字母池有其他 eligible 词，Then apple 不被选中
- AC-F006-02：Given 某字母池全部词均在冷却中，When 按下该字母，Then 解冻距 eligible 最近的词正常出词（敲键必有响应）
- AC-F006-03：Given 高优先级词与普通词同时 eligible，When 统计大量抽选结果，Then 高优先级词出现频率显著更高
- AC-F006-04：Given 词表 intervals 被修改，When 重启应用，Then 按新间隔序列调度

### F-007 词表加载

- AC-F007-01：Given 词表 JSON 合法，When 启动，Then 全部词加载成功
- AC-F007-02：Given 词表 JSON 语法错误，When 启动，Then 显示明确错误提示（含出错位置），不白屏
- AC-F007-03：Given 某词条缺非关键字段（如例句），When 加载，Then 该词其余功能正常可用

### F-008 一词一图

- AC-F008-01：Given 断网状态，When 出词，Then 图片正常显示（纯本地）
- AC-F008-02：Given 同一词任意两次出现，When 展示，Then 图片完全相同
- AC-F008-03：Given 图片被替换为新文件，When 重启应用，Then 显示新图（换图只需替换文件）

### F-009 家长配置页

- AC-F009-01：Given 配置每轮 10 词并开始游戏，When 第 10 词序列结束，Then 自动进入结算页
- AC-F009-02：Given 输入非法值（0、负数、非数字），When 保存，Then 拒绝并提示合法范围
- AC-F009-03：Given 配置已保存，When 刷新页面，Then 配置项保留

### F-010 结算页

- AC-F010-01：Given 本轮完成 N 词，When 第 N 词序列结束，Then 进入"今日已结束"页：庆祝动画 + 本轮单词回顾 + 总积分
- AC-F010-02：Given 结算页，When 孩子按字母键，Then 不再出词
- AC-F010-03：Given 结算页，When 查看全部可见元素，Then 不存在任何"再玩一次/继续"类入口；家长长按左上角 3 秒方可返回配置页开始新一轮

### F-011 适度动画

- AC-F011-01：Given 出词，When 卡片出现，Then 播放弹入 + 首字母高亮动画，总时长 ≤1s
- AC-F011-02：Given 积分 +1，When 计分板更新，Then 播放 +1 飘分反馈
- AC-F011-03：Given 动画播放中，When debounce 拦截按键，Then 动画不被打断、不叠加错乱

### F-012 稀有字母兜底（Should）

- AC-F012-01：Given X 池仅有 contains 类词（fox），When 按 x，Then 正常出 fox 并高亮词中字母 x
- AC-F012-02：Given 某字母同时有 initial 与 contains 词 eligible，When 选词，Then initial 类优先

### F-013 全屏防误触（Should）

- AC-F013-01：Given 进入游戏，When 家长开启全屏，Then 浏览器 UI 隐藏、画面占满屏幕
- AC-F013-02：Given 全屏中，When 孩子按 Esc/功能键，Then 游戏画面保持，不回到桌面（浏览器能力范围内尽力）

### F-014 点击重播（Should）

- AC-F014-01：Given 当前词序列播放完毕，When 点击图片或喇叭按钮，Then 重读完整序列
- AC-F014-02：Given 重播进行中，When 再次点击重播，Then 重新从头播放，不产生叠音

### F-015 本轮进度条（Should）

- AC-F015-01：Given 本轮目标 10 词、已完成 3 词，When 查看进度条，Then 显示 3/10
- AC-F015-02：Given 最后一词完成，When 进度条满格，Then 播放满格动画后进入结算页

## 7. 假设与约束

### 7.1 假设

- 单词清单、优先级、例句及例句中文均由家长提供（结构见 §4.5）
- 图片由家长在词表确定后批量准备；在此之前 MVP 使用 emoji 占位图，一词一图固定
- 孩子玩时有家长陪同或可及时求助，因此"全屏防误触"允许尽力而为
- localStorage 数据丢失（积分清零）属于可接受风险

### 7.2 约束

- 仅 macOS 本地使用，一台设备，一个孩子
- 完全离线，无账号、无网络请求（运行期）
- 内容与代码分离：词表 JSON + 图片目录

### 7.3 风险

| 风险 | 影响 | 概率 | 缓解措施 |
|------|------|------|----------|
| TTS 音色因 macOS 版本/浏览器不同而不可控 | 中 | 高 | 构建期在目标机器实测，选定 voice 名写入配置；配置页留音色下拉 |
| 真实图片到位前体验偏素 | 低 | 高 | MVP 用 emoji 占位图保证风格统一；词表确定后家长批量替换 images/ 目录即可 |
| 词表未到位阻塞开发 | 高 | 中 | 先用 5 字母 × 5 词的示例词表联调，真实词表到位后替换 |
| 孩子触发系统快捷键退出游戏 | 低 | 中 | 全屏 + 家长陪同；浏览器级无法完全屏蔽，接受 |
| localStorage 被清理丢数据 | 低 | 低 | 接受；可选导出 JSON 备份（列入 Could） |

## 8. 开放问题

已确认（2026-07-18）：

- 图片：MVP 阶段先用占位图，词表确定后家长批量准备
- 例句中文：家长随清单一并提供
- 词表规模：以家长实际清单为准，调度算法与规模无关
- 结算页：锁定式设计，页面无任何可见继续入口（家长隐藏手势除外）
- 稀有字母：自动用 contains 策略补词（fox→x）

待办：

- [ ] 家长提供正式词表（结构见 §4.5）

## 附录

- 调度算法设计见 §4.4；词表 schema 见 §4.5
- 教育依据备忘：4 岁+ 字母-单词配对曝光符合语音意识发展阶段；大写字母与键盘视觉一致；无对错信号时采用曝光计数调度而非经典 SRS
